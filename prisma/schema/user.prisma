model User {
    id Int @id @default(autoincrement())
    username  String  
    email String @unique 
    password String 
    role Role @default(client)
    profilePictureUrl String?
    bio String? @db.Text
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
    // Here comes releation fields
    createdJobs Job[]
    @@map("user")
    @@index([email])
    
}

enum Role {
    freelancer
    client
    admin
}